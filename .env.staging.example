# Example staging connection (do not commit real credentials)

# Prisma
DATABASE_URL=postgresql://commissable_staging:<password>@<staging-host>:5432/commissable_crm_staging
DIRECT_URL=postgresql://commissable_staging:<password>@<staging-host>:5432/commissable_crm_staging

# Shadow DB for Prisma migrate dev (never point this at prod)
SHADOW_DATABASE_URL=postgresql://commissable_staging:<password>@127.0.0.1:5432/commissable_crm_shadow

# Next.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-me"

# Cloud SQL (optional if using connector/proxy)
CLOUD_SQL_CONNECTION_NAME="groovy-design-471709-d1:us-central1:commissable-sql"
DB_USER="commissable_staging"
DB_PASSWORD="<password>"
DB_NAME="commissable_crm_staging"
USE_CLOUD_SQL_CONNECTOR="false"

