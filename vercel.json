{
  "name": "commissable-crm",
  "framework": "nextjs",
  "installCommand": "npm ci --omit=dev",
  "buildCommand": "npx prisma generate && npm run build",
  "env": {
    "NODE_ENV": "production"
  }
}
